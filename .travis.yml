language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t discorev/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true discorev/docker-react npm run test -- --coverage
